version: "3.9"

name: "poker"

services:
  web:
    image: nginx:latest
    command: /bin/sh -c \
      "envsubst < /etc/nginx/conf.d/default.conf.template > /etc/nginx/conf.d/default.conf && \
      nginx -g 'daemon off;'"
    depends_on:
      - auth
      - balance
      - collection
      - file
    env_file:
      - ${PWD}/.env
    ports:
      - ${PORT}:${PORT}
    networks:
      - like_network
    volumes:
      - ${PWD}/nginx:/etc/nginx
